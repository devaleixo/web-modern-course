const [a, b, c, d] = [3, 5, 7, 15]

const soma = a + b + c + d
const subtracao = d - c
const divisao = d / c
const multiplicacao = b * a
const modulo = d % c

console.log(soma, subtracao, Math.floor(divisao), multiplicacao, modulo)